The app contained an encrypted flag, and I needed to decrypt it.
### Solution:
I found the decryptText function in the app's code and called it with the encrypted string.

```python
# Use IDA or any disassembler to find the decryptText function
decryptText(encrypted_string)

# The decrypted string contains the flag
flag-decryptedValueHere
```
### Flag Found:
`flag-decryptedValueHere`
